spring:
  application:
    name: account-service
  cloud:
    config:
      uri: http://${CONFIG_SERVICE_SERVER}:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
